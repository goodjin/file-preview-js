# 实现计划: 默认 JavaScript 编程语言

## 概述

修改智能体系统的提示词配置文件，使所有智能体在编程任务中默认使用 JavaScript 而非 Python。

## 任务

- [x] 1. 修改基础提示词 (base.txt)
  - [x] 1.1 在 `[超出模型能力的处理]` 部分之前添加 `【默认编程语言（重要）】` 部分
    - 添加明确的 JavaScript 默认语言声明
    - 说明 JavaScript 优先于 Python
    - 列出不同场景的语言选择指导
    - _需求: 1.1, 1.2, 1.3_

- [x] 2. 修改根智能体提示词 (root.txt)
  - [x] 2.1 更新 `【技术约束传递原则（重要）】` 部分
    - 将现有的 "默认使用HTML、JS在前端实现" 扩展为更详细的说明
    - 明确前端任务使用 HTML + JavaScript
    - 明确后端/脚本任务使用 Node.js
    - 添加 "禁止默认使用 Python" 的声明
    - _需求: 2.1, 2.2, 2.3_

- [x] 3. 检查点 - 验证修改
  - 确保修改后的提示词文件语法正确
  - 确保新增内容与现有内容风格一致
  - 如有问题请向用户确认

## 备注

- tool_rules.txt 和 workspace.txt 当前示例已使用 JavaScript，无需修改
- 所有修改仅涉及文本内容，不涉及代码逻辑
- 修改后建议进行人工审查确保提示词清晰无歧义
