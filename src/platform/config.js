/**
 * 兼容性导出：config.js 已移动到 utils/config/config_loader.js
 * 
 * 此文件提供向后兼容的导出，以确保现有代码仍然可以从原路径导入。
 * 
 * @deprecated 请使用新路径：import { loadConfig } from "./utils/config/config_loader.js"
 */

export { loadConfig } from "./utils/config/config_loader.js";
